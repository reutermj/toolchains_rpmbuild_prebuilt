name: Test (aarch64)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Build test RPM
        run: bazel build //tests/rpm:hello-rpm
